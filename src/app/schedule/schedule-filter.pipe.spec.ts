import { ScheduleFilterPipe } from './schedule-filter.pipe';

describe('ScheduleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ScheduleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
