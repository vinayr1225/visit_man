import { PendingFilterPipe } from './pending-filter.pipe';

describe('PendingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PendingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
